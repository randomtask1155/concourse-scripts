---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine

inputs:
- name: support-scripts

run:
  path: /bin/bash
  args: ["./support-scripts/get-interfaces.sh"]

#run:
#  path: ls
#  args: ["-l", "./support-scripts"]
