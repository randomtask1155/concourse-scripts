---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine

inputs:
- name: support-scripts

run:
  path: ./support-scripts/get-interfaces.sh

#run:
#  path: /bin/bash
#  args: ["-c", "./support-scripts/get-interfaces.sh"]

#run:
#  path: ls
#  args: ["-l", "./support-scripts"]

#run:
#  path: ls
#  args: ["-l", "/bin"]
