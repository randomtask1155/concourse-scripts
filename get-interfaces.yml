---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine

run:
  path: /sbin/ifconfig
  args: ["-a"]

run: 
  path: /sbin/ip
  args: ["addr"]

run:
  path: /bin/netstat
  args: ["-rn"]
 
